{
  "name": "@charpoints/api",
  "version": "0.1.6",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "tsc --build --clean",
    "build": "tsc",
    "watch": "tsc -w --noEmit",
    "prebuild": "yarn clean",
    "prepack": "yarn build",
    "test": "yarn clean && jest --collectCoverage"
  },
  "devDependencies": {
    "jest": "^26.5.2",
    "ts-jest": "^26.4.1",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "@charpoints/core": "^0.1.0",
    "axios": "^0.21.0"
  }
}
